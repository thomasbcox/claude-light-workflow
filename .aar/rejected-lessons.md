# Rejected workflow lessons

Append-only. Kept so the reasoning survives — see `reviews/lesson-proposals.md` §6.
Read at exactly one moment: the novelty check in `/close` step 3b. Nothing else reads this.

No entries yet.

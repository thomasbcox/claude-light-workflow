{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "description": "Shared schema for design-altitude review \u2014 used by both the frame-time design-sketch review and the review-time approach pass. Distinct from finding-schema.json, which the correctness pass uses.",
  "properties": {
    "verdict": {
      "type": "string",
      "description": "Required, even when there are no findings. Frame: is this sketch a sound, modern way to satisfy the spec \u2014 and if not, what shape would you build instead? Review: if you built this from the spec yourself, would you build it this way? Name the simpler/declarative/library-based design when one exists."
    },
    "regressions": {
      "type": "array",
      "description": "Adversarial regressions the reviewer proposes, one or more per acceptance criterion, so the author writes tests against a list they did not author. Populated ONLY at the frame-time design pass, where no test or implementation exists yet; the review-time approach pass reuses this schema and returns an empty array here.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "criterion": {
            "type": "string",
            "description": "Which acceptance criterion this targets \u2014 its number or a short quote identifying it."
          },
          "regression": {
            "type": "string",
            "description": "A plausible way an implementation could satisfy this criterion's LETTER while violating its INTENT. Derived from the criterion itself, never from an intended implementation shape (a selector, an assertion, a file token) \u2014 none exists yet."
          }
        },
        "required": [
          "criterion",
          "regression"
        ]
      }
    },
    "findings": {
      "type": "array",
      "description": "Empty array if the shape is sound and idiomatic. Approach pass: at most the 3 highest-leverage concerns.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "severity": {
            "type": "string",
            "enum": [
              "BLOCKER",
              "IMPORTANT",
              "QUESTION",
              "NIT"
            ],
            "description": "{{contract:Severity labels}}"
          },
          "reversibility": {
            "type": "string",
            "enum": [
              "one-way",
              "two-way"
            ],
            "description": "{{contract:Classify#reversibility}}"
          },
          "standing": {
            "type": "string",
            "enum": [
              "standard",
              "nonstandard",
              "dated",
              "kludgy"
            ],
            "description": "{{contract:Classify#standing}}"
          },
          "title": {
            "type": "string",
            "description": "Short label for the finding."
          },
          "locus": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where it lives: a design-sketch section at frame time, or a file:line / module at review time. Null for a finding about absent code (something that should exist but doesn't)."
          },
          "claim": {
            "type": "string",
            "description": "What is structurally suboptimal / substandard, and why it matters."
          },
          "alternative": {
            "type": "string",
            "description": "The simpler / more declarative / more idiomatic design that would replace it."
          },
          "win": {
            "type": "string",
            "description": "{{contract:Best-practice assessment#Concrete win, not novelty.}}"
          }
        },
        "required": [
          "severity",
          "reversibility",
          "standing",
          "title",
          "locus",
          "claim",
          "alternative",
          "win"
        ]
      }
    }
  },
  "required": [
    "verdict",
    "findings",
    "regressions"
  ]
}
